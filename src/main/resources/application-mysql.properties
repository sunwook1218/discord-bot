# database init, supports mysql too
database=mysql
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost/discordbot}
spring.datasource.username=${MYSQL_USER:discordbot}
spring.datasource.password=${MYSQL_PASS:discordbot}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
